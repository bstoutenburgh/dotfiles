# TODO: Do not hardcode x64
---
- name: install slack
  become: yes
  apt:
    deb: >-
      https://downloads.slack-edge.com/releases/linux/{{ slack_version }}/prod/x64/slack-desktop-{{ slack_version }}-{{ slack_machine_map[ansible_machine] }}.deb
